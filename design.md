# 道具識別の画面

`index.html` が `styles.css` と `js/app.js` を読み込む。絞り込みと候補の描画は `js/view.js` が行う。

## 文字

フォントは `"DotGothic16", "Yu Gothic UI", monospace` である。`index.html` が Google Fonts から DotGothic16 を読み込む。

`styles.css` が決める大きさは次のとおりである。

- 本文は 16px、行間 1.45
- ページ見出しは 24px。幅 880px 以上では 28px
- コントロール見出しは 16px
- 種類・状態・チップ・ナッジのボタンは 16px
- リールの段は 18px
- 候補の見出しは 20px
- 候補の道具名は 18px
- 価格箱の見出しは 14px、数字は 16px
- 識別トグルは 22px

## 色

`:root` の変数は次のとおりである。

- `--gb-darkest`: `#1a2612`
- `--gb-dark`: `#3d4c2a`
- `--gb-light`: `#86945a`
- `--gb-lightest`: `#c5d59a`
- `--gb-shell`: `#d5d2c8`
- `--gb-bezel`: `#3a3e38`

ページ地は `--gb-shell` である。液晶面は `--gb-lightest` である。縁は `--gb-bezel` の 12px 枠である。枠の内側に `#2a2e28` の 3px 線がある。

走査線は液晶面の上に重なる。3px 透明のあと 1px の薄い緑である。

## 配置

`body` の余白は 12px である。`#app` の内側余白も 12px である。

狭い画面では、絞り込みと候補が縦に並ぶ。段の間隔は 12px である。

幅 880px 以上では、左に絞り込み、右に候補の 2 列になる。左列は `minmax(280px, 340px)` である。列の間隔は `1.25rem` である。絞り込みは `position: sticky` で上端から `1rem` に留まる。

## 種類

種類は 草、巻物、腕輪、壺、杖の 5 つである。各ボタンはアイコンとラベルを持つ。選んだ種類は `--gb-darkest` の地に `--gb-lightest` の文字である。

## 状態

状態は常に 通常、祝福、呪いの 3 つである。各操作は `role="switch"` のボタンである。含めるときは `aria-checked="true"` である。含めないときは `aria-checked="false"` である。見える文言は状態名に「含める」または「含めない」を付けたものである。

含める通常は `--gb-darkest` の地に `--gb-lightest` の文字である。含める祝福は `--gb-lightest` の地に `--gb-darkest` の文字と、内側の 3px 枠である。含める呪いは 45 度のディザである。含めない状態は `--gb-light` の地に `--gb-darkest` の文字である。

## 容量と残り回数

壺では容量チップが出る。杖では残り回数チップが出る。ほかの種類ではこの塊は隠れる。

先頭は「指定なし」である。そのあとに、その種類の道具の設定値範囲から集めた整数が並ぶ。押した値は `setParameter` に送られる。

## 買値と売値

買値と売値は縦リールである。いちばん上は「指定なし」である。その下に安い順の値段が並ぶ。

同時に有効な価格は片側だけである。片側で値段を選ぶと、もう片側は「指定なし」に戻る。

段の高さは 48px である。中央の段は反転する。ホイール、ドラッグ、上下キー、上下の三角で 1 段ずつ動く。

## 候補

候補の買値は `--gb-darkest` の地に `--gb-lightest` の文字の箱である。売値は `--gb-lightest` の地に枠付きの箱である。

含める状態が 2 つ以上のとき、候補は状態ごとの行に分かれる。識別済みの行は `--gb-lightest` の地になる。右端のトグルが識別済みの記録である。
